{
  "name": "shepherd-repo",
  "private": true,
  "license": "MIT",
  "scripts": {
    "lint:spec": "spectral lint openapi/shepherd.yaml --fail-severity=error",
    "lint:events": "spectral lint openapi/shepherd-events.asyncapi.yaml --ruleset .spectral-asyncapi.yaml --fail-severity=warn",
    "lint:specs": "npm run lint:spec && npm run lint:events"
  },
  "devDependencies": {
    "@apisyouwonthate/style-guide": "^1.5.0",
    "@ibm-cloud/openapi-ruleset": "^1.33.13",
    "@stoplight/spectral-cli": "^6.16.3",
    "@stoplight/spectral-owasp-ruleset": "^2.0.1"
  }
}
