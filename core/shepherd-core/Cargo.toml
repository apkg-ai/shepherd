[package]
name = "shepherd-core"
description = "shepherd domain core (v1 scaffold shell — rebuilt from step 001 onward)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]

[dev-dependencies]
