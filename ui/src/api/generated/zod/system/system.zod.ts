/**
 * Generated by orval 🍺
 * Do not edit manually.
 * shepherd API
 * Local-first hub for agent-driven project work. Maps projects as typed task graphs and acts as persistent shared memory across agent and human sessions. The spec is the product boundary — anything not in this contract does not exist. Design rules in docs/03-api.md.
 * OpenAPI spec version: 0.1.0
 */
import * as zod from "zod";

/**
 * Liveness probe per draft-inadarei-api-health-check-06. Returns 200 with status `pass` while healthy, 503 with status `fail` when degraded. Content-Type: application/health+json.
 * @summary Service health check
 */
export const getHealthResponseVersionMin = 5;
export const getHealthResponseVersionMax = 20;

export const getHealthResponseVersionRegExp = new RegExp("^\\d+\\.\\d+\\.\\d+(-[a-z0-9.]+)?$");
export const getHealthResponseDescriptionMax = 500;

export const getHealthResponseDescriptionRegExp = new RegExp("^\\S[\\s\\S]*$");
export const getHealthResponseOutputMax = 5000;

export const getHealthResponseOutputRegExp = new RegExp("^[\\s\\S]+$");
export const getHealthResponseNotesItemMax = 2000;

export const getHealthResponseNotesItemRegExp = new RegExp("^[\\s\\S]+$");
export const getHealthResponseNotesMin = 0;
export const getHealthResponseNotesMax = 50;

export const GetHealthResponse = zod
  .object({
    status: zod
      .enum(["pass", "warn", "fail"])
      .describe(
        "Service status. `pass` = healthy, `warn` = healthy with concerns, `fail` = unhealthy.",
      ),
    version: zod
      .string()
      .min(getHealthResponseVersionMin)
      .max(getHealthResponseVersionMax)
      .regex(getHealthResponseVersionRegExp)
      .describe("Public version of the service (SemVer)."),
    description: zod
      .string()
      .min(1)
      .max(getHealthResponseDescriptionMax)
      .regex(getHealthResponseDescriptionRegExp)
      .optional()
      .describe("Human-friendly description of the service."),
    output: zod
      .string()
      .min(1)
      .max(getHealthResponseOutputMax)
      .regex(getHealthResponseOutputRegExp)
      .optional()
      .describe(
        "Raw error output for unhealthy status. Present only when status is `fail` or `warn`.",
      ),
    notes: zod
      .array(
        zod
          .string()
          .min(1)
          .max(getHealthResponseNotesItemMax)
          .regex(getHealthResponseNotesItemRegExp),
      )
      .min(getHealthResponseNotesMin)
      .max(getHealthResponseNotesMax)
      .optional()
      .describe("Informational notes about the service."),
  })
  .describe(
    "Service health report per draft-inadarei-api-health-check-06. Content-Type: application/health+json.",
  );
